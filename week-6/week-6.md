# Week 6
## To compile and run python code, run python (progamename).py in terminal. Instead of make and ./.
## Slower programming running time yet faster human read and write time.
## In python we call headerfiles as packages and libraries as modules, as collection of functuions
## To input the cs50 packages you use from cs50 import (modulename), by this you precisely loaded that specific library from the packages saving memory. Such as, from cs50 import get_string.
## To seacrh up packages for python visit docs.python.org as their manual pages.


### Functions in python
#### For week 5, You can use set() to replace the need of implementation of hash table and the hash function. 
#### For week 4, there is a PIL package, after using from PIL, you use import Image, image filters for modules including box blurring, edge detection etc.
### You can use a named parameter in python, unlike positional ones, when you pass the value, arugment you can just specify what field the function should treat it as so you don't need to remember the order of them, for example in print you can do print ("(text)", end="") to get rid of the default \n for printing messages.


## Reflection
### Why exactly is python a slower lanuage if all programming lanuages ultimate will be translated to one?
#### Because python is an interpreter lanuage, so that it is not limited by a specific hardware to load, and can skip the time for checking the entire file again and to create a .exe of it, while this causes the requirement of letting computer to translate command line by line and actively move forward rather than done everything at once. Which this problem is called runtime overhead.
