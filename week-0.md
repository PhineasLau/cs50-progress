# Week 0
## 1. This course teaches me computer thinking to generate correct output and how to use some tools, along with the ability to use programming lanuage such as scratch, python, java script, c, html, css.
## 2. Not allowed to use any ai to help, except the rubber duck of CS50 that guide rather than give direct answer. CS50.ai. Which help to spot logical problems by simply verbal  communication as it organised the ideas. 
## 3. All programs must come from learners themselves.

## Base system
### Uses pattern of number to represent larger value/signal with less placeholders/memory needed, the number of the base system indicates the values avaliable for each placeholder, such as base 10 = each placeholder can have 10 different values (0-9), it also indicate how the value of each placeholder increases by each increase its power by 1. For example 123, the 1 is in hundreds place so representing 1 times by 100, which is 10^2, 2 is in tenth place representing 2 times by 10, which is 10^1. Deceimal means base 10, binary means base 2, urnry menas base 1.
### Binary
#### We uses binary as it is easier to communicate with computers with only two different values, 0 and 1, which by not giving, or giving electrcity, off or on to switches called transistors. Rather than detecting 10 different levels of voltages. It also follows the same base rule. Such as 001 represent 1, 100 represent 4 and 111 represent 7, by each placeholder means an increase power of 2, 2^2,2^1,2^0, times either 0 or 1. As 111 means 1*2^2 + 1*2^1 + 1^2^0 = 7. Each placeholder of binary is called bit, while 8 bits equal to a byte. A byte can count up to 255 and represent 256, 2^8 different values when we add all 0s representing 0 in. The maximum amount of different values available is indicates by the amount of bit. Such as 8 bits being 2^8 = 256, and 4 bits being 2^4 = 16.

## Representations
### To represent different types of signal other than just numbers, we uses different cheat sheets to universally agree on one number representing each of different data. Same values can represent different data based on different standardised system.
### Character representation
#### ASCII uses 7 bits, the value of 65 to represent A, and 66 to represent B and so on, while 65 and 66 would be 1000001 and 1000010 in 7 bits binary. So when we send text messages we are sending multiple set of binary 7 bits for others to recieve and they uses ASCII to decode each to show letters not numbers. For each captial letter, its number representation to its lowercase version has a difference of 32, which means that by switching 0 in 32th placeholder to 1 we turn that to its lowercase, such as 1000001 being A and 1100001 being a. Which extended ASCII can include more of 8 bit per character, which so gives a total of 256 different possible characters. Unicode can uses 8, 16 and up to 32 bits to represent different emoji and different lanuages symbols etc. Companies can also further interpret further based on the value of the bits, such as a tears of joy emoji across different devices share same unicode, yet can have different font to finally present out.
### Colour representation
#### We uses RGB standardised system to represent colour by mixing different ratio of red,green and blue, which each strength of the colour is represented by a byte, so you can a maximum of 256 different strength of red or 255 being the strongest and same for other two colours to mix and create other colours. There each colur of dots of an image, a pixel will takes at least 3 byte or 24 bits to represent.

## Algorithms
### To input and output data we uses different ways of representation system, the middle phase between an input and an output is processing, which we use algorithms as a step by step instruction to solve problems. Not only algorithms should solve the problem, but should be in the most efficent, time or resource saving way, which different algorithms and different coding lanuages against different sort of problems the best and most efficent method varies based on each focused areas. An example would be, for searching algorithm binary searching, of splitting items in half and half would be far superior for searching from a large amount of items compared to one by one, as even the amount doubles the time and resource needed only increase by little whereas the one by one takes double as well, therefore to invent and find better algorithm to solve different problem is one of the main focus of computer science. It is also every important to ensure that all situations are covered in the algorithm, if an unexpected conditions were met and computers have no instructions of what to do it will freeze.
### Pseudocode
#### Everyone can come up with their own representation of pseudocode using everyday lanuages, in structure, just like algorithms it is made up by functions, which solve the small problems, with conditions like boolean expressions, true or false, 1 or 0 questions, with finally loops. 
### Levels of codes
#### To actually command an computer we need to write code, which is read by computers in the lowest level way of 0s and 1s, however to write in 0s and 1s is extremely tedious and hard to read and maintain, which at the past is by dotting papers, so people invented higher level lanuages, that use a custom set of words to map out patterns of instruction and to translate to binary by compiler, some higher level lanuages are like C and even higher level of lanuages like python, that kept upgrading for even more user friendly by simply assembling those building blocks.
### Algorithm design
#### Start with an input, for example what you put in the say block in scratch, known as arugement, whereas the block itself is the process, the function, the coding part. And finally the output is the result that is to presented to users is known as the side effect. Whereas it could also store a variable at the output, and to pass it on for another function as it became the input of it, this result is known as return value. Which we should use a variable taken from users for input for the algorithm to act differently based on condition. Such as what to say in the say block or whether to process by another function. Which makes algorithm becomes the combination of small solutions to a biger problem. To maximise the effciency of avoiding repeatedly write and debug many same functions of an algorithm, we use loops and defining our new functions. Which defining new functions combine multiple processes together, allowing us to reuse same code quick and easily, without the need of knowing what the mechanic behind the function itself, creating a higher level of lanuage.  




