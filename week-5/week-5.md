# Week 5
## Data structures
### Queues has a property of FIFO, first in first out. Meaning that the first data enters the structure should be the first that leaves as they lasted the longer. Enqueue means to get in to the start of the line, and dequeue means to get out from the last of the line. We can impelment this in a simple way by declaring a data struct with a list and a integar tracker of current size, however the list itself needs a predeclared number as its maximum size, this means that the queues itself cannot grow with the data amount. We also need to track the which position is the current last data at to be push out next in the queue for the FIFO feature by adding data to start of array and taking from current last position. Which ensure in the case of data being taking out and putting back to the queue, all data will have been selected once before having the first repeated one.
### Stacks has a property of LIFO, last in first out. Meaning that the last data enters the structure should be the first that leaves just like how the newest emails/clothes/food trays stays on the top although this means older ones often never gets picked up because the newer ones keep getting replenished. Push means to push a data to the top stack, and pop means to take data out from the top stack. We can impelment this in a simple way just as how we implemented queues, we also use the data struct of list and a integar track of where the last data is in, so to be taken out and then a new data can replace that position later whereas the first data went initially stays.
### The problem of fixed size data structures can be solved by allocating and deallocating memory.

## Reallocation
### When we use malloc to return an address to a pointer with a fixed size of memory in byte, we may need to resize it, larger for more data or less to save memory. To do this we need reallocate the memory, the process is to reuse malloc on a new pointer for a different size then to copying everything from old to new pointer using a for loop, then to free the old pointer memory by free(), we could then make old pointer equal to new, as so reference to any one of them lead to the same address and so we effectively reused the pointer. The reason why we need an extra pointer is that malloc may fail to return a piece of memory and returns NULL, which means we will lost the track of the address to old piece of memory before resizing it, we need to detect the NULL, free the old pointer then to close the program by a return. 
### We can use the realloc() function from stdlib.h to automatically run the copy and paste process, by typing (new pointer) = realloc((original pointer),(new size in byte)), though we still need to detect whether it instead returned NULL and at the end free() the old pointer cause it would rises a risk of memory leak if we just forget it and as we now have a new pointer with same content but more available spaces.

## Dictionaries
### A table separated by keys and values, used to link pieces of data together, known as key value pairs. This is a data structure that often being used as data type. 

## Linked lists
### In the process of using malloc to get a chunk of memory, sometimes it would be difficult to find a continuous large piece of memory for the pointer, and there will many smaller pieces of memory between gaps of chunks of memory being unused, to use memory more efficiently, we can use a starter pointer, that contain the address of the first data, which the first data contain the address to the next data, and next data contain the one after it, repeatedly by this we have successfully linked smaller pieces of memory together without them being need to be back to back.
### To implement the units of linked lists, we can use a struct node, which we delcare a new data type by typedef called struct node, that to contain the main value in one field, then a pointer in another field, as we need to declare the data type for the value that will be store by that address, that data type will be struct node itself, as that data type should contain its value and a pointer address to contain the next same data structure. At the end we offically name the entire type as node, so no need of retyping struct node.
### Then to connect the nodes together and add values to each, we first create a starter as a pointer to node type of value, that doesn't need malloc as it is only just an address and if we used malloc then we will lose track of that peiece of memory as we update the address of starter pointer, then storing NULL as its value initally to clear out garbarge value to stablise it. Now we use a for loop, to create a node variable by malloc and as always to check whether that successed, then let it to store user input value, then makes its second field to be NULL then to be starter pointer's address, and the starter address points to the node variable. By this we ensure that the first node must have a pointer lead to the original list that having value of NULL, then the nodes afterward have the pointer to the previous one known as starter, because the last/starter at this point is no longer original starter address but the address to last node.
### To gain access to the values by the linked lists, we can use a for loop or a while loop, we ultilise the fact that the first ever list in linked list have a value as the original starter, NULL. So to create a new pointer ptr as node type and as current starter address, then as long as ptr is not NULL, print the dereferenced ptr value at the value field, and then make ptr as address of dereferenced of ptr what in the next address field. This repeats, as ptr is the address of last list, it print out what is in that address, then because that address has two values, it check the second value, make itself points to there, and the destination contain the value and the next until reach the first one as NULL. 
### Sometimes we may want to not create a new, but to append nodes to a pre created linked lists, to do this we can first detect whether the starter address is NULL, because starter should always change to point to last and so NULL means the list is empty, if so we just do the same as before, starter address = node address, otherwise if not, then to use a for loop to loop through linked list as ptr just like how we printed out values, then when the ptr next field address is NULL we add node as a memory address pointer to there and break.
### We can also append nodes in an order by its value field, so the largest one on last of the linked lists. To do this we simply going to check whether starter is NULL, if so just append starter as node, but if not then to check whether the current node value field is less than the starter ones, if so then to make the current node pointer address field as starter, then starter as address to current node. By this we make starter connect to current node, and current node have a field containing the original starter which is the address of the other node. A data may also happens should be at the later of the linked lists, then we can use a for loop, to travel through the linked lists by ptr being starter, if next field is NULL that means it meet the end and so set at the point ptr next field being node and break, otherwise if it is in the middle which when node number is less than ptr next field address's number, node next becomes ptr next, ptr next field becomes node and break. This so we successfully sort from smallest to largest number stored in value field.
### At the end of using linked list, just like fixed amount memory array, we also need to free all memory used from this dynamic list, because it is indefinite for length we should use a while or a for loop, to make ptr as starter, then as long as ptr is not NULL, set a new pointer as next being ptr next field address, free ptr, and ptr become next, because starter stores an address of another lists that stores the address of another, and so we let ptr to travel through the linked lists by next field value of each address and free it while next will help store where it should go next otherwise the moment you clear that memory out you forgot where to go next as well. Though there is a chance that the program fails to run malloc at one point and if we only included the free part after it success then some memory will be lost track if failed, so we should make this as a function declared outside and to run after it finish, or as soon as it fails.
### Although the idea of linked lists helps use memory more efficiently, that even fragmented memory can be used, but itself take more space because a node not only contains the value but an address and the searching, appending and depending of lists all takes big O(n) time which is slower than O(logn) of binary search for normal arrays that are stored memory back to back. 
### We can use the -> operator to dereference the pointer, so to access to value(s) inside there, and you place the field on right side, such as n->next;, means dereference n for both field and go to field for next, (*n).next.

## Trees
### A data structure that combines the advanatges of normal arrays and linked lists, that being able to apply binary search for higher speed yet can growth or shrink memory usage by data amount. A binary search tree uses a 2D family tree like structure, top being the middle sorted value, that cut one large list to two smaller, spread out to two other values, each being middle of their new smaller list, as each value cut their new list to even smaller lists, each of them spread out to two more values being the middle of the even smaller lists and so on. They follow the pattern for binary search, as value at left must be smaller than it parents, and opposite for the value at right. The searching process is recursive as to either go right or left until found value, and since it follows the nature of binary search, its searching time is therefore O(logn), faster than linked lists because each action halfs the amount of data needed to be searched. Because of how each leaf in the tree spread to two others, then it means each nodes contain three information, one as its value and two as pointers to other nodes. Though this leads to the downside of even more memories are needed as each node stores three value instead of two like linked lists or one like normal array.
### To implement the units used for binary search tree, we can reuse the same code for nodes for linked lists, typedef struct node, also one field for main value, and instead of one pointer we declare two field for pointers, one called left and one called right. 
### To apply binary search on the tree we can declare a recursive function that takes in a pointer parameter and a target value parameter, the pointer should be as the root of the tree, then if tree is NULL then return false, because it indicates that the tree is either empty or target value doesn't exist in there, else then we pass on the tree's left or right field pointer to itself and the same target value and return that result based on whether the target value is less than or greater than the tree value, otherwise if equal then return true. So this we successfully recall the same function inside itself acting as a step by step sequence of moving down a leaf a time. The reason why we return false on NULL is because the bottom leaves have no pointers toward other value therefore we put NULL in there and this always indicate the end of the tree and so we know we couldn't find the target value.
### The logic to build a tree in the most simpliest way is to get user input then to compare to last value(s), if less than it then place on left field otherwise right field. However sometimes a tree may devolve from O(logn) to O(n) because of user typing in ascending values, because of the structure of left and right by last value, ascending values will result in the tree becoming a one sided linked tree, which is simply a linked lists and so have all the disadvantage of low speed back, this therefore will require advanced algothrithm to automatically readjust the root of the tree dynamically to avoid the devolve.

## Hash
### We would want to avoid O(n) and perfer O(logn) as to let running time grow slower, but the best, ultimate holy grail of data structure is to achieve O(1), which never grows and always takes a constant amount of steps, which so running time is independent to the data amount in the data structure. The building block of it is hashing, which means to take an infinite amount of input as domains, and maps it to finite amount of output as ranges.
### The process of hashing can often be created as a mathematical function, such as turning the first letter of people's names into number and return out, which so generate an code for that name to be put at a group and so to be quickly searched up in a phonebook. Which so we will have generate a hash table to map the values as like a dictionary. However sometimes items could have the same code, such as when people having the same first letter in their name, and so we will use a link lists to stitch those names together under the same group, this is known as collisons. Over time as more collisons appear the program will slowly devole to O(n) just like linked lists, to avoid this we can add more reading to the hashing process to reduce chances of collison, such as turning the first three letters of people's names into number and output as code, though this will quickly explode the amount of memory needed and wasted as we will need to consider way more combinations to increase the amount of group available and so each fewer names ends up in the same group.

## Tries
### To constantly achieve O(1), we can use tries, which similar to trees, however the root of it would be an array that contains all possible digits or letters from a to z for a data structure used to store names, each elements in the array would be a node that also contains the pointer to another array of the same length. So in the example of searching a name, it would ask whether there is a name started with the letters first array confirms recorded before, then for all the letters that first letter lead to, to see if any match the second letter in the name and so on and until the last you can add a tracker for whether that letter have ever been recorded as end of a name, if so and it match the length of the searching name you found the name successfully. For example to search a name called Toad you search whether in the root there is anyone named start with T, then for everyone name started with T to see whether there is anyone name started with o and so on until when on name length [3] to see if d exists and if that has been the end of a name before. It can always achieve O(1), as more combination recorded doesn't affect the search process. But in the downside on much much more memory needed, such as each node for letters contains an array for 26 elements, each of them contains another array with 26 elements and so on. Which can cause an insane amount of memory unused and wasted, and slows the computer down by taking useful memory

## Reflection
### A pointer can points to multiple data by making it points to a data structure that contains multiple fields like a pointer to a node, but is there a limit of how many fields a pointer can have, how many values an address can hold in that block of memory, and are they actually all stores in one giant block of memory marked by that address or are there multiple fixed size blocks, byte of memory?
### Although we often associate pointer as an address to hold a large chunk of memory, but in fact pointers only point to the first block, the first byte of memory, the way pointer stores multiple bytes of values is by having many blocks of memory back to back and there are no limit to how many blocks, how many values a pointer can hold, as it only points to the first byte anyway, as long as you have enough continuous blocks of memory. And the way computers know start from which block is for the next field/value is by using offset, because it already knows how many bytes the first field takes, so it knows the blocks after it are for the next fields, and so to use the amount of blocks second fields takes to know when third starts etc, this repeats until the computer knows it is the end of all fields by knowing how many byte the entire data structure takes.

### Projects
#### 1. inheritance.c
##### A program to simulate family inheritance of blood type, it will show it in the form of a family tree, and you will be able to observe how the blood types of earilest generation influence the latest generation, although it only shows three but you can easily edit it to show even more. On the surface it mainly trains the skill of travelling between pointers, but it also let us to repractice the use of recursive function, as you use a recursive function to travel to eariler generation, spread out like a tree, then to finally give the earilest ones their random blood type.

#### 2. Speller.c
##### A surprisingly complex program that to check the spellings of texts based on whether each word exist in dictionary. This has struggled me for a long period of time because of many misassumption of how to achieve a feature. But overall it strengthen my understanding of memory allocation, designing hash function , memory traversal, and most importantly, to design a generally good program by balancing memory usage and effciency and performance against a specific case.
##### The first struggle I met was not even knowing how to start, I was in the conflict of not knowing whether I should try to understand every parts before I start, I knew the key information about it, like hash table is array of buckets, each buckets contains the address to a node and each node points to next, but the actual implementation is like hitting a blank wall, because I tend to start with all the tools I needed before starting to use them, for example the hash function, otherwise if I just throw myself into a large function the amount of wrong assumptions would force me to start over, then I finally realised that I focused on the wrong subject, I first thought check() was too complicated, so I reduced that to load by watching the video for it, but actually I should start this from the very very scratch, how should I design my hash function.
##### The default hash table was based on the first letter of the word, so 26 buckets, and I realised that the less buckets there are, more collison happens because more words squeezed to one bucket, and this means higher running time, and the more buckets there are, more memory would be used as each buckets even empty still takes 8 bits of memory and more memory would be wasted because some buckets may ended up empty, while it would be faster. So I considered using the first two characters instead, first thought that 26^2 amount of buckets would be enormous, which it is much larger than 26 buckets but each pointers only take tiny amount memory anyway. To implement the first two letters base system I first experiment it with first two digits for first character and rest for second, like aa = 0000, ab = 0001, za = 2500, but then I realised that although it is easy to read but difficult to put in an array, as we should have only 26^2, 676 amount of buckets but now we have 2525 of them as there are gaps between each group, instead of abandoning I decided to improve from it, the gap happens by every 26 characters, then I removed the gaps, making it back 676 buckets, then I finally learned that because every 26 characters a group, deviding the new no gap index by 26 gives the first character, then the reminder indicates the next, however my goal was to turn a word to a hash code, not a hash code to a word, so I reverse engineered that and worked out that by muliplying the first character's position in the alphabet by 26, then add the second character's we produce 0-675 with no gaps in between, this ultimately fixed the issue of gaps caused by the first design using a different, corrdinate system to the array index by simply addition. 
##### Now I have the tool I need to implement load, I was making it far more difficult than it needs to be I thought I need to determine the length of every word manually using fread, iterate until \0, add the null terminator, somehow construct the node by moving the internal text cursor back, after struggling for a long time I discovered fscanf(), that basically do all of them above for me, I now learned that many small features may actually have implemented as a library before, even though I was never explicitly taught fscanf, but I do know scanf, so rather than just using the exact material I have, I should also try to look up for something similar, scaning not from keyboard but file, befroe forcing myself doing the same thing as it did.
##### I then easily created my first node before iterating the process, by knowing the first node next field points to nothing, and so I implemented my first version of appending nodes to the very end, it works but when I later reviewed it I learned that this requires me to go to the end every time like a queue when I actually didn't need to.
##### I then need to finish size(), which should returns the words loaded from dictionary, but instead of traverse every single words, I made the decision of simply setting up a global counter, update everytime right after a word pass all error checking and added to hash table. So I just let that size function returns the global variable, which is way more efficient way to approach this final result.
##### I finally need to unload all words I loaded, I avoided falling into the trap of walking through the hash table and freeing each on the way, because my for loop only put me to the next index after what inside that for loop iteration is done, which means if I free that node, then I will lost the pointer field values that tell me where is the next word is, so I created a buffer pointer to store that next field value for current node before freeing the current node, and because of how that buffer is an address to store only one data type so I didn't use malloc and as it lives on the stack so it will auto free itself after program finished, and I believe this demostrated a pretty nice understanding of linked-list memory management. 
##### Now I have all the tools I can implement check(), and it was way way easier than I thought, compared to how I was confused at the start, this is all thanks to decompositing tools to break down to smaller parts and abstracting the logic by simply knowing what the output should be and read them directly as I now have a clear idea what they should do instead of over focsuing on how. So I finished the logic of check by reusing hash function, go to that hashed bucket then go through each fully stored word field value, and compare them by the word parameter by strcmp(). 
##### The logic was smooth until check50 failed almost everything, then I reread how it said it is not case insensitive, which should be the most easy part to investigate cause there are only hash function and check function invlove them, then rereading hash I have already used tolower as I know upper and lower case have different ASCII values, so I quickly locked in to check(), learned that strcmp is case sensitive, that means user may type a lowercase word, while in dictionary that was a lowercase version, also because of hash tolower they are in same bucket but that tolower didn't extend to when I directly compare them, so I searched up a new function, changed strcmp to strcasecmp and added headerfile strings.h, not string.h.
##### After everything worked and gives green ticks, I go back to optimise my linked list insertion, from appending by walking to the end wasting running time I just insert at front. I attached my new node next field to points to the bucket, then let bucket becomes the address to my new node by literally assigning it with equal sign, so now the table is pointing to the new node and that new node next field points to original table that points to next node so nothing is lost.
##### After submitting it I also thought about what beyond it, like based on three characters, then there would be 26^3, 17,576 buckets, initially thought that sounded huge and will waste many memory for combinations like zzz like literally 676 more times of memory needed compared to just 26 buckets. Then I realised that a bucket is basically just a pointer. So tens of thousands of bucket pointers aren't really going to take that much memory, as least when this can save the time for comparing tens of thousands of complete nodes that not only slower but actually may have a chance of taking more memory, because a bucket is a back to back chunk of memory given by malloc, while linked lists although in a bucket look like continuous but not, each of them are a malloc plus a connection pointer. Then I tried to use the same two letters approach for three, by multiplying by 26 and add the last, but so aza and zaa would be the same sum because of how multiplying the first two letters by same 26 and add them together with normal last letter would commutative, different character positions need different weights if order is supposed to matter. At the end I didn't implement the three-character version because I recognised that finishing the next assignments when I already took so much time on it was more important than endlessly optimising an already-working solution.
##### Overall I went from understanding a hash table to actually constructing one, in terms of pointers I strengthen my skill of using them by referencing nodes, connecting linked lists, traversing lists, preserving the next node before freeing memory, I learned the strcasecmp, designing my very own hashing function, and investigate the error in my code using the clues it provided to select the one to check for. And at last it teaches me to break down a problem, again and again to actual smallest pieces, no need to force myself to reunderstand how, but what for each tools.
